packages:
  - "apps/*"
  - "packages/*"

# pnpm blocks dependency lifecycle scripts by default. These packages were
# explicitly reviewed because their install/build steps are required by the
# toolchain. Additions require security review.
onlyBuiltDependencies:
  - "@swc/core"
  - "esbuild"
  - "protobufjs"
  - "sharp"
